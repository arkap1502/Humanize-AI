# Optional: point the frontend at a deployed backend.
# Leave empty/unset for GitHub Pages — the app humanizes locally in the browser.
# REACT_APP_API_URL=https://your-backend.onrender.com
REACT_APP_API_URL=
